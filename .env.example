# ── Database ───────────────────────────────────────────────────────────────
# Local development uses a SQLite file (zero setup):
DATABASE_URL="file:./dev.db"
#
# For production / deployment, switch the `provider` in prisma/schema.prisma to
# "postgresql" and point this at a managed Postgres connection string, e.g.:
#   DATABASE_URL="postgresql://USER:PASS@HOST/db?sslmode=require"

# ── Gmail integration (optional) ──────────────────────────────────────────────
# The Job Application Tracker reads your inbox to detect "Applied / Interview /
# Assessment / Offer" emails. Until these are set, the app shows sample data.
#
# How to get them:
#   1. Go to https://console.cloud.google.com/ and create a project.
#   2. Enable the "Gmail API".
#   3. Create an OAuth 2.0 Client ID (type: Web application).
#   4. Add this Authorized redirect URI:
#        http://localhost:3000/api/gmail/callback
#   5. Copy the Client ID + Client Secret below.

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3000/api/gmail/callback
